document.addEventListener('DOMContentLoaded', () => {    const chessBoard = document.getElementById('chessBoard');    const pieces = {        r: '♜', n: '♞', b: '♝', q: '♛', k: '♚', p: '♟',        R: '♖', N: '♘', B: '♗', Q: '♕', K: '♔', P: '♙'    };    // Create an 8x8 chessboard    const createBoard = () => {        let isLightSquare = true;        for (let row = 0; row < 8; row++) {            for (let col = 0; col < 8; col++) {                const square = document.createElement('div');                square.className = `square ${isLightSquare ? 'light' : 'dark'}`;                square.setAttribute('data-row', row);                square.setAttribute('data-col', col);                chessBoard.appendChild(square);                isLightSquare = !isLightSquare;            }            isLightSquare = !isLightSquare;        }    };    // Initial setup of pieces    const setupPieces = () => {        const initialSetup = [            ['r', 'n', 'b', 'q', 'k', 'b', 'n', 'r'],            ['p', 'p', 'p', 'p', 'p', 'p', 'p', 'p'],            [' ', ' ', ' ', ' ', ' ', ' ', ' ', ' '],            [' ', ' ', ' ', ' ', ' ', ' ', ' ', ' '],            [' ', ' ', ' ', ' ', ' ', ' ', ' ', ' '],            [' ', ' ', ' ', ' ', ' ', ' ', ' ', ' '],            ['P', 'P', 'P', 'P', 'P', 'P', 'P', 'P'],            ['R', 'N', 'B', 'Q', 'K', 'B', 'N', 'R'],        ];        for (let row = 0; row < 8; row++) {            for (let col = 0; col < 8; col++) {                const square = document.querySelector(`.square[data-row='${row}'][data-col='${col}']`);                if (initialSetup[row][col] !== ' ') {                    const piece = document.createElement('div');                    piece.className = 'piece';                    piece.textContent = pieces[initialSetup[row][col]];                    square.appendChild(piece);                }            }        }    };    // Create board and setup pieces    createBoard();    setupPieces();    // Add basic click event for moving pieces (drag and drop functionality can be added)    let selectedPiece = null;    chessBoard.addEventListener('click', (event) => {        const target = event.target;        if (target.classList.contains('piece')) {            selectedPiece = target;        } else if (selectedPiece && target.classList.contains('square')) {            target.appendChild(selectedPiece);            selectedPiece = null;        }    });});